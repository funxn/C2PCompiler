#include<stdio.h>
int gcd(int a,int b)
{
if(b==(0))
{
return (a);
}
else
{
return (gcd(b,a%(b)));
}
}
int main(int argc,char** argv)
{
//input,output
int x,y;
int get(int a,int bnt b))
{
get(int a,int bnt b))=(b + (a));
}
scanf("%d%d",&x,&y);
printf("%d",gcd(x,y));

return 0;
}